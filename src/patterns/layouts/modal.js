import React from 'react'

function Modal(props) {
     return (
          <img
               style={{ height: '20px' }}
               src={props.image}
               alt='logo'
          />
     )
}

export default Modal
